<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WindowSill.ClipboardHistory.Views.EmptyOrDisabledItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:api="using:WindowSill.API"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToVisibilityConverter
            x:Key="InverseBoolToVisibilityConverter"
            FalseValue="Visible"
            TrueValue="Collapsed" />
    </UserControl.Resources>

    <Grid>
        <StackPanel
            Spacing="8"
            Visibility="{x:Bind ViewModel.IsClipboardHistoryEnabled, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <HyperlinkButton
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                Command="{x:Bind ViewModel.OpenWindowsClipboardHistoryCommand}">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    api:Uids.Uid="/WindowSill.ClipboardHistory/Misc/EnableClipboardHistory"
                    TextAlignment="Center"
                    TextWrapping="WrapWholeWords" />
            </HyperlinkButton>
        </StackPanel>

        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            api:Uids.Uid="/WindowSill.ClipboardHistory/Misc/EmptyClipboard"
            TextAlignment="Center"
            TextWrapping="WrapWholeWords"
            Visibility="{x:Bind ViewModel.IsClipboardHistoryEnabled, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
